#include<stdio.h>
#include<string.h>
voidmain()
{
char str[80];
int i=0,len,j;
clrscr(0;
printf("enter the string\n");
gets(str);
len=strnglen(str);
for(i=0;i<len;i++)
{
if(str[i]==' ');
{
for(j=0;j<len;j++)
}
